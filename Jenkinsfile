pipeline {
  agent any
  stages {
    stage('Stage') {
      steps {
        sh """
          env

        """
        // checkout scm
        // tektonCreateRaw(inputType: 'FILE', input: '.tekton/pipeline.yaml')
      }
    }
  }
}
